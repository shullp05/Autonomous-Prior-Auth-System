# ============================================================================
# PriorAuth - Autonomous Prior Authorization System
# Requirements file - Compatible with Python 3.11+
# ============================================================================

# -----------------------------------------------------------------------------
# Core LangChain / LLM Framework
# -----------------------------------------------------------------------------
langchain>=1.0.0,<2
langchain-core>=1.0.0,<2
langchain-community>=0.4.0,<1
langchain-ollama>=1.0.0,<2
langchain-chroma>=1.0.0,<2
langchain-text-splitters>=1.0.0,<2
langgraph>=1.0.0,<2
langgraph-checkpoint>=3.0.0,<4
langsmith>=0.4.0,<1

# -----------------------------------------------------------------------------
# Vector DB & Embeddings
# -----------------------------------------------------------------------------
chromadb>=1.0.0,<2
# NOTE: BCEmbedding pins transformers<4.37.0, so we let it manage that dependency
BCEmbedding>=0.1.5,<1
sentence-transformers>=2.2.0,<4

# -----------------------------------------------------------------------------
# LLM Runtime
# -----------------------------------------------------------------------------
ollama>=0.6.0,<1
# transformers version managed by BCEmbedding to avoid conflicts
tokenizers>=0.15.0,<1
sentencepiece>=0.2.0,<1
safetensors>=0.4.0,<1

# -----------------------------------------------------------------------------
# ML / Deep Learning
# -----------------------------------------------------------------------------
torch>=2.2.0,<3
numpy>=1.26.0,<2
onnxruntime>=1.20.0,<2

# -----------------------------------------------------------------------------
# Data Processing
# -----------------------------------------------------------------------------
pandas>=2.0.0,<3
pydantic>=2.0.0,<3
pydantic-settings>=2.0.0,<3

# -----------------------------------------------------------------------------
# PDF & Document Processing
# -----------------------------------------------------------------------------
pypdf>=6.0.0,<7
reportlab>=4.0.0,<5
pillow>=10.0.0,<13

# -----------------------------------------------------------------------------
# HTTP & API
# -----------------------------------------------------------------------------
httpx>=0.28.0,<1
requests>=2.32.0,<3
aiohttp>=3.10.0,<4
fastapi>=0.115.0,<1
uvicorn>=0.30.0,<1

# -----------------------------------------------------------------------------
# Configuration & Environment
# -----------------------------------------------------------------------------
python-dotenv>=1.0.0,<2
PyYAML>=6.0.0,<7

# -----------------------------------------------------------------------------
# Schema & Validation
# -----------------------------------------------------------------------------
jsonschema>=4.20.0,<5
marshmallow>=3.20.0,<4

# -----------------------------------------------------------------------------
# Utilities
# -----------------------------------------------------------------------------
tqdm>=4.60.0,<5
rich>=14.0.0,<15
typer>=0.12.0,<1
coloredlogs>=15.0.0,<16
tenacity>=9.0.0,<10
packaging>=24.0,<26
psutil>=5.9.0,<7

# -----------------------------------------------------------------------------
# Testing & Development
# -----------------------------------------------------------------------------
pytest>=9.0.0,<10
pytest-cov>=6.0.0,<8
coverage>=7.0.0,<8

# -----------------------------------------------------------------------------
# Telemetry (optional, for tracing)
# -----------------------------------------------------------------------------
opentelemetry-api>=1.30.0,<2
opentelemetry-sdk>=1.30.0,<2
opentelemetry-exporter-otlp-proto-grpc>=1.30.0,<2

# -----------------------------------------------------------------------------
# Database / ORM (for future scalability)
# -----------------------------------------------------------------------------
SQLAlchemy>=2.0.0,<3
